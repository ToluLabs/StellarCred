# Rust's output directory
target

# Node
node_modules
.pnpm-store
.next
dist

# Local settings
.soroban
.stellar
.env.local

# Local tooling config (not committed)
.kilo/

# Benchmark output (local results; see BENCHMARKS.md for documented figures)
benchmark_results.txt

# Proptest regression files
proptest-regressions/

# macOS
.DS_Store
# Rust's output directory
target

# Node
node_modules
.pnpm-store
.next
dist

# Local settings
.soroban
.stellar
.env.local

# Deployment artifacts (local; use git for version control)
deployment-manifests/
*.wasm.hash

# Benchmark output (local results; see BENCHMARKS.md for documented figures)
benchmark_results.txt

# Proptest regression files
proptest-regressions/

# macOS
.DS_Store
